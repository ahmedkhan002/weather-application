export type Place={name:string;country:string;admin1?:string;latitude:number;longitude:number};export type Weather={current:any;hourly:any;daily:any;timezone:string};export async function places(q:string):Promise<Place[]>{if(!q.trim())return[];const r=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(q)}&count=6&language=en&format=json`);if(!r.ok)throw Error('Search unavailable');const j=await r.json();return(j.results??[]).map((x:any)=>({name:x.name,country:x.country,admin1:x.admin1,latitude:x.latitude,longitude:x.longitude}))}export async function forecast(p:Place):Promise<Weather>{const u=new URL('https://api.open-meteo.com/v1/forecast');u.searchParams.set('latitude',String(p.latitude));u.searchParams.set('longitude',String(p.longitude));u.searchParams.set('timezone','auto');u.searchParams.set('forecast_days','7');u.searchParams.set('current','temperature_2m,relative_humidity_2m,apparent_temperature,is_day,precipitation,weather_code,wind_speed_10m,surface_pressure,visibility');u.searchParams.set('hourly','temperature_2m,precipitation_probability,precipitation,weather_code,wind_speed_10m,relative_humidity_2m,uv_index');u.searchParams.set('daily','weather_code,temperature_2m_max,temperature_2m_min,sunrise,sunset,uv_index_max,precipitation_sum,precipitation_probability_max,wind_speed_10m_max');const r=await fetch(u);if(!r.ok)throw Error('Weather unavailable');return r.json()}export function icon(code:number){if(code===0)return'☀';if(code<=3)return'◒';if(code<=48)return'◌';if(code<=67)return'⌁';if(code<=77)return'❄';if(code<=82)return'◈';return'ϟ'}export function label(code:number){if(code===0)return'Clear sky';if(code<=3)return'Partly cloudy';if(code<=48)return'Foggy';if(code<=67)return'Rain';if(code<=77)return'Snow';if(code<=82)return'Showers';return'Thunderstorm'}export const fmt=(iso:string,opts:Intl.DateTimeFormatOptions)=>new Intl.DateTimeFormat(undefined,opts).format(new Date(iso));